# 동행이 케어 콘솔 — 프런트 디자인 명세

> 어드민(보호자용 콘솔) 프런트엔드의 아트 디렉션 고정 문서.
> 구현 시(사람이든 AI든) 이 문서가 제약이다 — 여기서 벗어나는 디자인 판단은 하지 않는다.
> 목적: 흔한 어드민 템플릿으로 미끄러지지 않고, "동행이"다운 화면을 일관되게 만드는 것.

---

## 1. 제품 정의

- **이름**: 동행이 케어 콘솔
- **사용자**: 보호자 (40~60대, 비개발자). 노인 당사자가 직접 보는 화면은 아니지만,
  가족이 함께 보는 상황을 가정해 **노인 친화 수준의 가독성**을 기본값으로 한다.
- **한 문장 목표**: "보호자가 말 한마디로 동행이의 행동을 바꾸고, 안심하고 배포한다."
- **성격 키워드**: 따뜻함 · 신뢰 · 단순함. (반대말: 차가운 SaaS 대시보드, 정보 과밀, 개발자 용어)

## 2. 아트 디렉션

### 무드

병원이 아니라 **거실**. 데이터 콘솔이 아니라 **돌봄 노트**.
차트·그래프·숫자 위젯을 최대한 배제하고, 문장과 카드로 말한다.
참고 무드: Intercom Fin의 Guidance(자연어 규칙 카드), 채널톡 설정 화면의 여백,
토스의 "쉬운 한국어" 라이팅.

### 금지 목록 (템플릿 미끄러짐 방지)

- 보라→파랑 그라데이션 히어로, 다크 네이비 관리자 템플릿
- 이모지를 섹션 제목/메뉴 아이콘으로 사용 (아이콘은 라인 아이콘 한 세트로 통일)
- 통계 카드 4개 나란히 (숫자 대시보드 금지 — 이 제품의 v1에는 통계가 없다)
- 개발 용어 노출: "프롬프트", "tool call", "파라미터", "웹훅" → 각각
  "지침", "동행이가 할 수 있는 일", "필요한 정보", "외부 연결"로 표기
- 회색 #808080류 무채색 — 모든 회색은 웜 그레이(아이보리 쪽으로 기운 회색)만 사용

## 3. 디자인 시스템 (토큰)

### 색 — 노인 친화: 고대비 + 따뜻한 저채도

| 토큰 | 라이트 | 용도 |
|---|---|---|
| `--bg` | `#FAF7F1` | 페이지 바탕 (따뜻한 아이보리) |
| `--surface` | `#FFFFFF` | 카드, 사이드바 |
| `--ink` | `#2B2722` | 본문 텍스트 (웜 블랙, 대비 12:1) |
| `--muted` | `#6E6659` | 보조 텍스트 (대비 4.5:1 이상 유지) |
| `--line` | `#E7E0D5` | 구분선, 카드 테두리 |
| `--accent` | `#1E7A63` | 주 액션 버튼, 활성 메뉴 (동행이 그린 — 아키텍처 문서와 동일 계열) |
| `--accent-soft` | `#E4F0EB` | 활성 배경, 선택 상태 |
| `--warn` | `#B4552D` | 위험/주의 (배포, 삭제 확인) — 빨강 대신 따뜻한 주황갈색 |
| `--warn-soft` | `#F7E9E1` | 주의 배경 |

- 다크 모드는 v1 범위 아님 (보호자 사용 맥락상 라이트 고정, 나중에 토큰만 추가)
- 의미색(성공/주의)은 accent와 별개로 운용. 페이지당 강조색은 accent 하나만.

### 타이포그래피

- **서체**: Pretendard (로컬 포함), 폴백 `-apple-system, "Apple SD Gothic Neo", sans-serif`
- **기본 크기 17px** (노인 친화 — 일반 어드민의 14px 금지), 행간 1.7
- 스케일: 28(페이지 제목) / 20(섹션) / 17(본문) / 15(보조) / 13(라벨, letter-spacing 0.05em)
- 숫자가 정렬되는 곳(버전, 시각)은 `tabular-nums`

### 형태·간격

- 모서리 12px (카드), 10px (버튼·입력) — 통일. `rounded` 값 혼용 금지
- 그림자 최소: 카드 `0 1px 3px rgba(43,39,34,.06)` 하나만. 호버 시 테두리 색 변화로 대응
- 터치 타깃 최소 44×44px, 버튼 높이 48px (노인 친화 기준을 어드민에도 적용)
- 간격 단위 8px 그리드: 8/16/24/40

### 컴포넌트 규칙

- **버튼**: 주 버튼(accent 채움, 흰 글자) 화면당 1개 원칙. 나머지는 테두리 버튼
- **지침 카드**: 좌측에 3px accent 세로선, 문장 그대로 표시, 우측에 삭제(휴지통) 아이콘
- **토글**: iOS 스타일 스위치 (체크박스 금지 — 켬/끔이 명확해야 함)
- **모달**: 화면 중앙 480px, 배경 딤 40%. 파괴적 동작(삭제·배포)은 반드시 모달로 한 번 더 확인
- **빈 상태**: 모든 리스트는 빈 상태 문구 필수 ("아직 등록된 연락처가 없어요. 첫 연락처를 추가해 보세요.")

### 라이팅 (words = design)

- 존댓말, 짧은 문장. 버튼은 동사로 끝: "배포하기", "지침 추가", "연락처 등록"
- 확인 모달은 결과를 말한다: "v3으로 배포하면 동행이가 바로 새 지침대로 행동합니다."
- 에러는 원인+해결: "전화번호 형식이 올바르지 않아요. 010-0000-0000 형태로 입력해 주세요."

## 4. 레이아웃

```
┌──────────┬──────────────────────────────────────────┐
│ 사이드바   │  콘텐츠 (max-width 760px, 좌측 정렬)        │
│ 240px    │                                          │
│ (접으면    │  페이지 제목 (28px)                        │
│  72px,   │  한 줄 설명 (muted)                        │
│  아이콘만) │  ───────────────────────                 │
│          │  섹션들…                                   │
│ 하단:     │                                          │
│ Live v2  │                                          │
│ 상태 뱃지  │                                          │
└──────────┴──────────────────────────────────────────┘
```

- **사이드바 (토글 접힘 지원)**: 상단 로고 "동행이 케어 콘솔" → 메뉴 5개 → 하단에 현재 배포 상태 뱃지(`Live v2 · 수정 3건`) 상시 노출. 이 뱃지를 누르면 배포 페이지로 이동
- 메뉴: **시나리오 / 동행이의 할 일 / 알림 연락처 / 배포 / 활동 기록**(v2, 비활성 표시)
- 콘텐츠는 한 컬럼. 2컬럼 그리드는 시나리오 카드 목록에서만 허용
- 모바일(<768px): 사이드바는 햄버거로 오버레이. 콘솔 특성상 태블릿까지만 최적화

## 5. 화면별 서사 (섹션 역할)

각 화면은 "요약 → 목록 → 추가"의 동일한 서사를 따른다. 화면마다 구조가 달라지면 안 된다.

### 5.1 로그인
- 중앙 카드 1장 (360px): 로고 → "보호자님, 어서 오세요" → 이메일/비밀번호 → [로그인]
- 배경은 `--bg` 단색. 일러스트/그라데이션 없음

### 5.2 시나리오 (홈)
- 서사: "동행이가 지금 어떤 상황을 지켜보고 있는지" 한눈에
- 시나리오 카드(낙상/복약/키오스크): 이름 + 켬/끔 토글 + 지침 개수 + 연결된 행동 칩
- 카드 클릭 → 상세: **지침 카드 리스트**(추가/삭제) + **[🎙 말로 추가]** 버튼(주 버튼)
  + "감지되면 → [행동 선택]" 연결 한 줄
- 말로 추가 플로우: 마이크 → 받아쓴 문장 표시 → "이렇게 등록할까요?" 확인 카드 → 추가.
  등록되는 것은 항상 draft — 사이드바 뱃지가 `수정 N건`으로 올라간다

### 5.3 동행이의 할 일 (액션 레지스트리)
- 서사: "동행이가 실행할 수 있는 행동의 목록" — 기존 형식 그대로 보여주고, 새 형식 추가
- 테이블: 이름 / 언제 하는 일인지 / 필요한 정보 / 종류(내장·외부 연결)
- [+ 새로 만들기] 모달: 이름 → 설명(도움말: "동행이가 언제 이 행동을 할지 판단하는 기준이에요")
  → 필요한 정보(행 추가) → 실행 방식: **보호자 알림 / 기록 남기기 / 119 신고 기록 / 외부 연결(URL)**

### 5.4 알림 연락처 (에스컬레이션 — "번호 등록")
- 서사: "위급할 때 누구에게, 어떤 순서로 알릴 것인가"
- 연락처 리스트: 이름 · 관계 · 전화번호, 드래그로 우선순위(1순위 뱃지)
- [연락처 등록] 인라인 폼 (모달 아님 — 자주 쓰는 동작이라 가볍게)
- 하단 규칙 한 줄: "[낙상 ▾] 감지 후 [30초 ▾] 응답 없으면 [1순위 ▾]에게 알립니다" (select 3개)

### 5.5 배포 (버전 관리)
- 서사: "무엇이 바뀌었는지 확인하고, 안심하고 내보낸다"
- 상단 비교 밴드: `Live v2` ←→ `Draft` 나란히
- **변경 사항 리스트가 이 화면의 주인공**: "지침 추가됨 — '낙상하면 딸에게 먼저 알려줘'",
  "연락처 등록됨 — 김OO (딸)" 처럼 사람 말로 된 diff. 항목별 되돌리기 링크
- [v3으로 배포하기] (주 버튼, warn색 아님 — 배포는 긍정 동작) → 확인 모달
- 버전 히스토리 테이블: v · 배포 시각 · 변경 요약 · [이 버전으로 되돌리기]

### 5.6 플레이그라운드 (전 화면 공통)
- 우측 하단 고정 버튼 "동행이와 테스트" → 우측 슬라이드 패널 (380px)
- 패널 상단에 항상 `Draft 버전으로 테스트 중` 뱃지 — Live와 헷갈리면 안 됨

## 6. 모션

- 전역 원칙: 위치 이동 애니메이션 금지, opacity/색 전환 150ms만
- 예외 2곳: 사이드바 접힘(200ms), 플레이그라운드 패널 슬라이드(250ms)
- `prefers-reduced-motion` 시 모두 제거

## 7. 접근성 (노인 친화 기준)

- 본문 대비 AA+ (4.5:1) 필수, 주요 텍스트는 AAA(7:1) 목표
- 색만으로 상태를 전하지 않기 — 켬/끔은 토글 위치+라벨, Live/Draft는 뱃지 문구
- 모든 인터랙션에 `:focus-visible` 아웃라인 (accent 2px)
- 폼 라벨 항상 노출 (placeholder를 라벨 대용으로 쓰지 않기)

## 8. 검증 루프

구현 중 매 화면마다 다음을 반복한다 (Playwright 기준):

1. 화면 렌더 → 스크린샷 (데스크톱 1280px, 태블릿 768px 두 벌)
2. 체크리스트 대조: 토큰 외 색 사용 여부 / 금지 목록 위반 / 폰트 크기 17px 미만 텍스트 /
   터치 타깃 44px 미만 / 빈 상태 누락
3. 위반 발견 → 수정 → 재렌더. 통과 전까지 다음 화면으로 넘어가지 않는다

## 9. v1 범위 (마감 기준)

| 우선 | 항목 |
|---|---|
| 필수 | 로그인, 시나리오 목록+지침 편집, 할 일 테이블+추가, 알림 연락처(번호 등록), 배포(변경 diff+버전 히스토리) |
| 권장 | 🎙 말로 지침 추가, 플레이그라운드 패널 |
| 다음 | 활동 기록(모니터링), 다크 모드, 대시보드 |
