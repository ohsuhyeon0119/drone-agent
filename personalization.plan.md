# 개인화 기획 — 보호자에게서 어르신 정보를 받아 에이전트에 반영하기

작성 2026-08-09 · 대상 `Drone Agent Final`

---

## 1. 문제

동행이의 시스템 프롬프트는 지금 두 층으로 되어 있다.

| 층 | 내용 | 어디에 있나 | 누가 정하나 |
|---|---|---|---|
| ① 에이전트 페르소나 | 이름, 말투, 팔다리 없음, 안전 원칙, **어떤 어르신을 만나든** 취할 태도 8가지 | `agent/persona.py` `PERSONA_BASE` | 코드 (고정) |
| ② 상황별 지침 | 시나리오별 감지 프롬프트와 대응 지침 | SQLite → `agent/scenarios/*.yaml` | 보호자 (콘솔) |

빠진 것이 **③ 지금 곁에 계신 분이 누구인가**이다.

①에는 "귀가 잘 안 들리시는 분은 이렇게, 기억이 흐릿하신 분은 이렇게" 하는 **일반론 8가지**가 있지만, 동행이는 **눈앞의 어르신이 그중 어디에 해당하는지 모른다.** 매 대화마다 추측해야 한다.

현재 어르신 정보의 전부:

```json
// drone-agent-app/memory.json
{ "profile": { "name": "오수현", "medication": "혈압약", "scheduled_time": "20:00" } }
```

이름, 약, 시간 세 개다. 그리고 이 값은 **아무도 입력한 적이 없다** — 코드에 적혀 있을 뿐이다.

### 진짜 문제는 스키마가 아니라 수집이다

필드를 늘리는 건 5분이면 된다. 어려운 건 **보호자가 로그인한 뒤 그 값을 실제로 채우게 만드는 것**이다. 이 문서는 거기에 대한 기획이다.

---

## 2. 설계 원칙

**원칙 1 — 프로필(사실)과 설정(동작)을 분리한다.**
"어머님이 귀가 어두우시다"는 **사실**이고, "낙상 감지 민감도를 0.6으로 낮춘다"는 **설정**이다. 보호자는 사실만 말하게 하고, 설정으로 바꾸는 건 시스템이 한다. 자녀에게 민감도를 묻는 순간 이탈한다.

**원칙 2 — 생성된 것은 제안일 뿐, 검토 없이 반영하지 않는다.**
LLM이 만든 지침이 바로 어르신에게 말을 걸게 두지 않는다. 다행히 **검토 화면이 이미 있다** — `admin_store`의 draft/publish 구조가 그대로 "이렇게 준비했어요, 맞나요?" 단계가 된다.

**원칙 3 — 3분 안에 끝내고 쓰기 시작하게 한다.**
처음에 다 받아내려 하면 아무도 끝까지 안 한다. 최소한만 받고, 나머지는 살면서 채운다. **모든 질문은 건너뛸 수 있어야 한다.**

**원칙 4 — 자녀가 아는 것만 묻는다.**
"어머님 인지 기능 수준이 어떻습니까"는 답할 수 없다. "같은 이야기를 여러 번 하시나요"는 답할 수 있다. **회상(recall)이 아니라 재인(recognition)** 으로 묻는다.

**원칙 5 — 프로필은 배포 대상이 아니다.**
"귀가 어두우시다"를 뒤늦게 알게 되어 고칠 때 배포 승인을 거치게 하면 안 된다. 프로필은 저장 즉시 다음 세션부터 반영된다. 반면 프로필로 **생성된 시나리오·지침**은 draft로 들어가고 배포를 거친다.

---

## 3. 수집 흐름 — 세 개의 통로

```
로그인
  │
  ├─ 1단계 · 기본 (30초)      폼 4칸        성함 / 나이 / 호칭 / 함께 사시는지
  │
  ├─ 2단계 · 카드 (30초)      카드 선택      무엇을 지켜볼까요
  │                                        낙상·복약·식사·외출·수면
  │
  ├─ 3단계 · 인터뷰 (2분)     대화형        동행이가 자녀에게 묻는다
  │                                        되물을 수 있는 유일한 통로
  │
  ├─ 4단계 · 사진 (10초)      업로드        처방전·약봉투 → VLM이 읽음
  │                                        (복약을 골랐을 때만)
  │
  └─ 5단계 · 검토             draft 화면     "이렇게 준비했어요" → 배포
```

각 통로가 맡는 역할이 다르다.

| 통로 | 왜 이걸로 받나 |
|---|---|
| 폼 | 이름·나이는 되물을 것이 없다. 폼이 가장 빠르다. |
| 카드 | **자녀는 무엇을 설정해야 할지 모른다.** 보여주면 고를 수 있다. 그리고 시나리오는 detect_prompt 품질이 중요해서 카탈로그에서 골라야 한다(§6). |
| 인터뷰 | **폼이 못 하는 게 되물어보기다.** "귀가 어두우세요" 한마디에서 "얼마나 어두우신지"를 캐낼 수 있고, 예상 못 한 정보("최근에 우울해하세요")가 나온다. |
| 사진 | 약 이름·용법·시간이 처방전에 다 있다. 타이핑할 이유가 없다. |

### 인터뷰 예시

> **동행이** — 어머님에 대해 조금 알려주시면 제가 더 잘 도와드릴 수 있어요. 평소에 말씀을 많이 하시는 편인가요?
> **자녀** — 별로요. 조용하세요. 귀도 좀 어두우시고.
> **동행이** — 많이 어두우신가요? 큰 소리로 말씀드려야 알아들으시는 편일까요?
> **자녀** — 네. 그리고 무릎이 안 좋으셔서 잘 못 움직이세요.
> **동행이** — 알겠습니다. 집에서 특히 조심하셔야 할 곳이 있을까요? 화장실이나 문턱 같은…
> **자녀** — 화장실 갈 때 제일 걱정돼요.

여기서 나온 것: 말수 적음 / 청력 나쁨 / 거동 불편 / 위험 장소 = 화장실. **네 항목 모두 페르소나와 감지 설정을 바꾼다.**

### 3.1 질문 목록 — 무엇이 정해져 있고 무엇이 아닌가

| | 정해져 있나 |
|---|---|
| 받아야 할 항목 (슬롯) | **고정** — 아래 표가 전부다 |
| 질문 문장 | **고정** — 아래 문장을 기본으로 쓴다 |
| 묻는 순서 | 자유 — 이미 답한 항목은 건너뛴다 |
| 되묻기 | 조건부 — 답에 따라 갈린다 |
| 자유 발화 수용 | 슬롯에 없어도 받는다 |

**질문을 LLM이 지어내게 두지 않는다.** 어르신 건강을 묻는 자리라 문장 하나가 실례가 될 수 있고, 매번 다른 걸 물으면 데이터가 안 모인다. LLM이 하는 일은 **무엇을 건너뛸지 고르는 것**과 **되묻는 것**이다(§3.2).

#### 폼 (2단계) — 4개 고정

1. 성함이 어떻게 되세요?
2. 몇 년생이세요? *(연세보다 출생년도가 정확하다)*
3. 동행이가 뭐라고 불러드릴까요? — `어르신` / `○○○ 님` / `할머니·할아버지` / 직접입력
4. 혼자 지내세요? — `혼자` / `배우자와` / `가족과`

#### 인터뷰 (4단계) — 필수 6개

문장은 표준 도구(AD8, 장기요양인정조사표, Morse Fall Scale)의 설계를 따랐다. 근거는 §16.

**순서가 중요하다.** 관찰하기 쉽고 감정 부담이 없는 것부터 묻고, **가장 무거운 `memory`를 맨 뒤에 둔다.** 인지 저하를 인정하는 건 자녀에게 힘든 일이라 앞에 놓으면 거기서 이탈한다.

| # | 슬롯 | 여는 질문 | 되묻기 (조건) |
|---|---|---|---|
| 1 | `mobility` | "혼자 화장실 다녀오시는 건 괜찮으세요?" | 불편 → "지팡이나 보행기를 쓰시나요?" |
| 2 | `fall_history` | "최근 1년 안에 넘어지신 적 있으세요?" | 있다 → "어디서 넘어지셨어요?" (`risk_places`)<br>**없다·모름 → "멍이 들었거나 어디 부딪히셨다는 말씀은요?"** |
| 3 | `hearing` | **"TV 소리를 크게 틀어두시는 편인가요?"** | 그렇다 → "말씀드린 걸 여러 번 되물으시는 일도 잦으세요?" |
| 4 | `talkativeness` | "평소에 말씀이 많은 편이세요, 조용하신 편이세요?" | 조용하심 → **"원래 그러셨어요, 아니면 예전보다 줄어드신 건가요?"** |
| 5 | `help_attitude` | "누가 도와드리려고 하면 어떤 반응이세요?" | 없음 |
| 6 | `memory` | "예전에 비해 같은 이야기를 자주 하시나요?" | 그렇다 → "약을 드셨는지 못 알아보시는 정도인가요?" |

**설계 원칙 여섯 가지.**

1. **상태가 아니라 장면을 묻는다.** "청력이 어떠세요"는 자녀가 답을 모른다. 매일 겪는 장면을 물어야 즉답이 나온다(원칙 4).
2. **절대 수준이 아니라 변화를 묻는다.** AD8의 8문항이 전부 *"예전과 비교해 변화가 있습니까"* 형태다.
3. **한 문항에 한 가지만 묻는다.** "걸어다니시는 건 어떠세요? 화장실은 혼자 가세요?"처럼 둘을 묶으면(double-barreled) 어느 쪽에 대한 답인지 알 수 없다.
4. **유도하지 않는다.** "편하게 받으시는 편인가요?"는 긍정 쪽으로 답을 몰아간다. "어떤 반응이세요?"로 열어둔다.
5. **"모르겠어요"를 항상 허용한다.** 자녀가 모르는 게 정상인 항목이 있다(§3.3).
6. **진단명은 묻지 않는다.** 필요한 건 진단명이 아니라 **행동**이다.

#### 문항별 판단 근거

**`hearing` — 자녀에게 직접 묻지 않고 TV로 우회한다.**
"보통 목소리로 말씀드리면 알아들으세요?"에는 함정이 둘 있다. 자녀는 **이미 크게 말하는 데 적응해서** 문제를 인지하지 못하고, 통화가 주된 접점이면 원래 잘 안 들리므로 답이 왜곡된다. **TV 음량은 관찰 가능하고 적응 편향이 없다.**

**`fall_history` — 자녀가 모를 가능성을 전제한다.**
**어르신은 넘어진 걸 자식에게 말하지 않는 경우가 많다.** 걱정 끼치기 싫어서, 또는 요양원 이야기가 나올까 봐. 그래서 "없다"는 답을 그대로 믿으면 안 되고, *"멍이 들었거나 어디 부딪히셨다는 말씀은요?"* 로 한 번 우회한다.

**`talkativeness` — 성향과 우울을 분리한다.**
원래 과묵하신 분과 **최근 말수가 준** 분은 완전히 다른 신호인데 첫 질문만으로는 같은 답이 나온다. 전자는 "과하게 말 걸지 않기"이고, **후자는 보호자에게 알려야 할 변화다.** 되묻기로 갈라낸다.

### 3.3 "모르겠어요"를 반드시 받는다

자녀가 모르는 게 정상인 항목이 있다 — 낙상 여부, 수면의 질, 혼자 계실 때의 상태. **억지로 답하게 하면 틀린 값이 들어온다.**

**틀린 데이터는 없는 데이터보다 나쁘다.** 없으면 기본값(`min_confidence` 0.7)으로 안전하게 돌지만, 틀리면 감지 민감도를 잘못된 방향으로 조정한다. "안 넘어지셨다"는 오답 하나가 낙상 위험 점수를 2점 깎는다(§16.2 ⑧).

- 모든 선택지에 **"모르겠어요"** 를 넣는다
- 모름은 **미응답으로 저장**한다 — 0점이 아니라 점수 산정에서 제외
- 프로필 페이지의 "아직 안 알려주신 것"(§9.2)에 올려 나중에 채우게 한다

### 3.4 자유 답변을 슬롯 값으로 옮기는 기준

인터뷰는 자유 텍스트인데 슬롯은 이산값이다. **"좀 어두우세요"가 '조금'인지 '많이'인지 모델이 임의로 정하게 두면 안 된다.**

`/extract` 프롬프트에 매핑 기준을 명시한다.

| 슬롯 | 값 | 판단 기준 |
|---|---|---|
| `hearing` | 많이 어두우심 | 큰 소리가 필요하다 / 되묻는 일이 잦다는 언급이 **있을 때만** |
| | 조금 어두우심 | 어둡다는 언급은 있으나 정도가 불명확 |
| | 잘 들으심 | 문제 없다고 명시 |
| `mobility` | 보행기·휠체어 | 보조기구 언급이 **있을 때만** |
| | 지팡이 | 지팡이 언급 |
| | 불편하심 | 불편 언급은 있으나 기구 언급 없음 |
| | 자유로움 | 문제 없다고 명시 |

**애매하면 낮은 쪽(덜 심각한 쪽)으로 판정하고 되묻기를 발동한다.** 과대 판정은 오알림으로 이어져 보호자 신뢰를 깎는다.

#### 조건부 — 2단계에서 고른 카드에 따라

| 카드 | 질문 |
|---|---|
| 복약 | "드시는 약이 있으세요? 언제 드시나요?" → **사진 업로드로 유도** |
| 복약 | **"약은 스스로 챙겨 드세요?"** (IADL 항목 — §16 인사이트 2) |
| 식사 | "식사는 규칙적으로 하시는 편인가요?" |
| 외출 | "밖에 나가시는 편인가요? 걱정되는 시간대가 있으세요?" |
| 수면 | "밤에 잘 주무세요?" |

#### 마무리 — 2개

1. "좋아하시는 게 있으세요? 이야기 나눌 때 꺼낼 만한 거요." → `interests`, `family`
2. **"제가 꼭 알아야 할 게 더 있을까요?"** → 자유

**마지막 질문이 가장 값어치 있을 수 있다.** 슬롯에 없는 것 — "요즘 우울해하세요", "밤에 자꾸 나가려고 하세요" — 이 여기서 나온다. 레벨 3 생성(§6)의 주 재료다.

총 **필수 5 + 조건부 1~2 + 마무리 2 = 8~9턴.** 이미 답한 슬롯은 건너뛰므로 실제로는 더 짧다. 종료 상한은 12턴으로 둔다.

### 3.2 그러면 LLM은 왜 필요한가

질문이 다 정해져 있다면 분기 폼으로도 만들 수 있다. LLM을 쓰는 이유는 셋이다.

**① 한 답에 여러 슬롯이 동시에 채워진다.**
> "별로요, 조용하세요. 귀도 좀 어두우시고."

`talkativeness`와 `hearing`이 한 번에 나왔다. 고정 순서 폼이면 **이미 답한 걸 또 묻는다.** 이게 가장 큰 이유다.

**② 되묻기가 답에 달려 있다.**
"조금 어두우세요"와 "거의 못 들으세요"는 다음 질문이 다르고, 후자는 `min_confidence`까지 바꾼다.

**③ 슬롯 밖의 말을 받는다.**
"밤에 자꾸 나가려고 하세요"는 어떤 슬롯에도 없지만 새 시나리오를 켤 근거다. 폼은 이걸 버린다.

**LLM 없이 간다면** — §3.1의 표를 그대로 조건부 폼으로 구현할 수 있다. 잃는 건 ①②③과 시연 임팩트다. 폴백으로 남겨둔다(인터뷰 API가 실패하면 폼으로 떨어진다).

---

## 4. 프로필 데이터 모델

필드마다 **"이게 있으면 무엇이 달라지는가"** 를 적는다. 답이 없는 필드는 넣지 않는다.

### 4.1 기본

| 필드 | 형태 | 무엇이 달라지나 |
|---|---|---|
| `name` | 문자열 | 페르소나 호칭 |
| `birth_year` | 숫자 | 나이 계산, 대화 소재(연배) |
| `address_as` | `어르신` \| `성함` \| `할머니/할아버지` \| 직접입력 | **페르소나 호칭 문장을 직접 대체** |
| `lives_alone` | 불리언 | 무응답 시 위급 판단 강도 |

### 4.2 감각·거동 — 페르소나 8가지 중 어디에 해당하는가

| 필드 | 선택지 | 무엇이 달라지나 |
|---|---|---|
| `hearing` | 잘 들으심 / 조금 어두우심 / 많이 어두우심 | 페르소나 1번 활성화. "천천히, 짧게" 강조 |
| `vision` | 좋으심 / 나쁘심 | 화면 설명 시 더 자세히 |
| `mobility` | 자유로움 / 지팡이 / 보행기·휠체어 / 거의 못 움직이심 | 페르소나 5번 활성화. **낙상 `min_confidence` 하향** |
| `memory` | 또렷하심 / 가끔 잊으심 / 자주 잊으심 | 페르소나 2번 활성화. **복약 지침에 "드셨는지 기억 못 하실 수 있음" 추가** |

### 4.3 성향 — 어떤 태도로 대할 것인가

| 필드 | 선택지 | 무엇이 달라지나 |
|---|---|---|
| `talkativeness` | 말씀 많으심 / 보통 / 조용하심 | 페르소나 3번 vs "과하게 말 걸지 않기" |
| `help_attitude` | 편하게 받으심 / 꺼리시는 편 | **페르소나 4번 활성화** — "제가 해드릴게요" 대신 "이렇게 하시면 됩니다" |
| `mood_recent` | 밝으심 / 보통 / 가라앉으심 | 페르소나 6번 활성화 |

### 4.4 건강

| 필드 | 형태 | 무엇이 달라지나 |
|---|---|---|
| `medications[]` | `{name, time, dosage, note}` | **복약 시나리오 지침에 그대로 삽입.** 사진에서 추출 |
| `conditions[]` | 자유 문자열 (당뇨, 고혈압, 관절염) | 페르소나 주의사항 |
| `cautions[]` | 자유 문자열 (단것 제한, 계단 금지) | 페르소나 주의사항 |

### 4.5 생활·관계 — 대화의 재료

| 필드 | 형태 | 무엇이 달라지나 |
|---|---|---|
| `daily_routine` | `{wake, sleep, meals[]}` | 시간 기반 신호(식사·취침 확인) |
| `family[]` | `{name, relation}` | **대화 소재** — "따님 수현 씨는 잘 지내시죠?" |
| `interests[]` | 자유 문자열 (트로트, 화투, 텃밭) | **대화 소재.** 외로우신 분께 말을 걸 실마리 |
| `risk_places[]` | 자유 문자열 (화장실, 문턱) | **낙상 detect_prompt 보강** |

### 4.6 저장

```sql
CREATE TABLE IF NOT EXISTS profiles (
  agent_id     INTEGER PRIMARY KEY REFERENCES agents(id),
  profile_json TEXT NOT NULL,
  updated_at   TEXT NOT NULL,
  updated_by   TEXT
);
```

`admin_store.py`의 `SCHEMA`에 추가. `config_versions`에 넣지 않는 이유는 원칙 5. 대신 수정은 `log_event("profile.updated", ...)`로 남긴다.

기존 `memory.json`은 **프로필 테이블에서 파생되는 캐시**로 격하한다 — `memory.py`가 프로필을 읽도록 바꾸고, 복약 로그만 계속 자기 파일에 쌓는다.

---

## 5. 프로필 → 페르소나 (레벨 1, 위험 0)

`persona.py`에 `build_profile_block(profile)`을 추가하고 `build_unified_persona()`가 `PERSONA_BASE` 바로 뒤에 끼워 넣는다.

### 렌더 결과 예시

앞의 인터뷰에서 나온 값으로 만들면:

```
[지금 곁에 계신 분]
김순자 어르신, 82세. "어르신"이라고 부르세요.
혼자 지내십니다.

- 귀가 많이 어두우십니다. 위 1번을 특히 지키세요 — 더 크게가 아니라 더 천천히,
  더 짧게 말합니다.
- 거동이 불편하십니다(보행기 사용). 위 5번에 따라 움직이시라는 안내는 신중히 하고,
  무리해 보이면 권하지 말고 보호자에게 알리는 쪽을 택하세요.
- 말수가 적으신 편입니다. 대답이 짧아도 재촉하지 말고, 과하게 말을 걸지 마세요.
- 도움받는 것을 꺼리시는 편입니다. 위 4번에 따라 "제가 해드릴게요"보다
  "이렇게 하시면 됩니다"라고 말하세요.

복용 중인 약: 혈압약(암로디핀) 저녁 8시 1정.
지병: 고혈압, 무릎 관절염.
조심할 것: 계단은 권하지 마세요.

가족: 따님 오수현. 좋아하시는 것: 트로트, 텃밭 가꾸기.
대화가 끊기면 이런 이야기로 자연스럽게 이어가세요.
```

**핵심은 일반론 8가지를 다시 쓰는 게 아니라 "몇 번이 이 분께 해당하는지"를 가리키는 것**이다. 프롬프트가 짧아지고, `PERSONA_BASE`와 내용이 어긋날 일이 없다.

### 구현

```python
def build_profile_block(profile: dict) -> str:
    """프로필을 페르소나 문장으로 렌더한다.

    PERSONA_BASE의 일반론(1~8번)을 다시 쓰지 않고 가리키기만 한다 —
    같은 내용을 두 군데 두면 한쪽만 고쳐졌을 때 모순된 지침이 된다.
    값이 비어 있으면 그 줄을 통째로 빼서, 아무것도 안 채운 상태에서도
    지금과 동일하게 동작하게 한다.
    """
```

빈 프로필이면 빈 문자열을 반환한다 → **온보딩을 건너뛴 사용자도 지금과 똑같이 동작한다.** 회귀 위험 없음.

호출부는 `main.py`의 `/ws/unified` 연결 시점. 이미 `load_scenarios()`를 매 연결마다 호출하고 있으므로 `get_profile()` 하나만 추가하면 되고, 서버 재시작 없이 다음 세션부터 반영된다.

---

## 6. 프로필 → 설정 (레벨 2·3)

### 레벨 2 — 규칙 기반 (예측 가능, 자동 적용 후 검토)

프로필 값 → 미리 정해둔 설정 변경. LLM 개입 없음.

| 조건 | 조정 | 근거 |
|---|---|---|
| `mobility` ∈ {보행기, 거의 못 움직이심} | 낙상 `min_confidence` 0.7 → 0.6 | 넘어질 확률이 높으니 놓치는 쪽보다 한 번 더 묻는 쪽 |
| `hearing` = 많이 어두우심 | 낙상 지침에 "세 번 물어도 답이 없으면 위급으로 보고 알린다" 추가 | 무응답이 못 들으신 것일 수 있음 |
| `memory` = 자주 잊으심 | 복약 지침에 "드셨는지 기억 못 하실 수 있으니 다그치지 말고 기록을 확인해 말씀드린다" 추가 | 페르소나 2번의 구체화 |
| `risk_places` 있음 | 낙상 `detect_prompt`에 "특히 {장소} 부근을 주의 깊게 보라" 추가 | 감지 정확도 |
| `medications` 있음 | 복약 시나리오 활성화 + 지침에 약 이름·시간 삽입 | |
| `lives_alone` = false | 낙상 지침에 "가족이 함께 계실 수 있으니 먼저 상황을 확인한다" | 오알림 감소 |

규칙은 `agent/personalize.py`에 표로 둔다. **코드가 아니라 데이터로** — 나중에 규칙을 늘릴 때 로직을 안 건드리게.

### 레벨 3 — LLM 생성 (반드시 검토)

인터뷰 자유 발화에서 규칙으로 못 잡는 것을 뽑는다.

> "요즘 자꾸 밤에 나가려고 하세요" → 야간 외출 시나리오 + 지침
> "물을 잘 안 드세요" → 수분 섭취 권유 지침

**생성 범위를 좁힌다.**

- ✅ **지침(instruction)은 생성한다** — 그냥 문장이라 틀려도 검토에서 걸러진다.
- ⚠️ **시나리오는 카탈로그에서 고르기만 한다** — `detect_prompt`는 오탐/미탐을 좌우해서 튜닝이 필요하다. LLM이 즉석에서 쓴 감지 프롬프트는 신뢰할 수 없다.

그래서 **시나리오 카탈로그**를 둔다. 우리가 미리 detect_prompt를 다듬어 둔 목록이고, 2단계 카드와 같은 데이터를 쓴다.

```
낙상 감지     ← 기본 켜짐
복약 확인     ← 기본 켜짐
식사 확인
외출·현관
장시간 정지
야간 배회
```

LLM은 "이 카탈로그 중 무엇을 켤지 + 어떤 지침을 붙일지"만 답한다. 카탈로그 밖은 못 만든다.

### 6.1 적용 추적 — 인터뷰 한 번이 어디까지 가나

답변이 닿는 곳은 **네 군데**다.

| # | 적용 지점 | 실제 파일 | 무엇이 바뀌나 |
|---|---|---|---|
| 1 | **페르소나 문장** | `persona.py` → 시스템 프롬프트 | 말투, 호칭, 대화 개시 빈도, 태도 |
| 2 | **감지 설정** | `scenarios/*.yaml` `min_confidence`, `detect_prompt` | VLM이 무엇을 어떤 기준으로 보나 |
| 3 | **대응 지침** | `scenarios/*.yaml` `instructions` | 감지된 뒤 무엇을 하나 |
| 4 | **알림 내용** | `tools.py` `notify_caregiver` 인자 | 보호자에게 뭐라고 전하나 |

#### 지금 상태 — 프로필이 프롬프트에 없다

`main.py:338`이 `build_unified_persona(scenarios, actions)`를 호출한다. **프로필 인자가 없다.**

그런데 복약 지침 첫 줄은 이렇다.

> "복약 시간 신호를 받으면 **처방 정보를 바탕으로** 먼저 다정하게 복용을 권합니다."

**참조할 처방 정보가 프롬프트 어디에도 없다.** `memory.get_profile()`은 `/api/remind-medication`(`main.py:133`)에서만 읽혀서, 사람이 버튼을 눌렀을 때 넛지 문장에 약 이름이 실려 들어올 뿐이다. 카메라로 복약을 감지한 경로에는 약 이름이 없다.

프로필 주입이 이 구멍부터 메운다.

#### 추적 — §3 인터뷰 예시가 만들어내는 변경

앞의 대화에서 나온 값: `talkativeness=적음` / `hearing=많이 어두움` / `mobility=보행기` / `risk_places=[화장실]` / `medications=[혈압약 20:00]` / `memory=가끔 잊으심`

**① 페르소나** — `PERSONA_BASE` 뒤에 `[지금 곁에 계신 분]` 블록이 붙는다(§5 렌더 예시). 감지가 한 번도 안 일어나도 **모든 대화의 말투와 호칭이 달라진다.**

**② 낙상 감지 설정**

```diff
  # agent/scenarios/fall.yaml
- min_confidence: 0.7
+ min_confidence: 0.6          # 거동 불편 → 놓치는 쪽보다 한 번 더 묻는 쪽

  detect_prompt: |
    ... (기존 유지) ...
+   이 어르신은 보행기를 쓰십니다. 보행기가 넘어져 있거나 어르신과 떨어져
+   있으면 낙상 가능성을 높게 보십시오.
+   화장실 부근을 특히 주의 깊게 보십시오.
```

**③ 낙상 지침**

```diff
  instructions:
    - text: 쓰러짐이 감지되면 즉시 걱정스러운 톤으로 "괜찮으세요?"라고 물어봅니다.
    - text: 사용자가 괜찮지 않다고 답하거나 응답이 없으면 보호자에게 알립니다.
      action: notify_caregiver
+   - text: 귀가 어두우셔서 못 들으셨을 수 있으니, 답이 없으면 더 천천히 세 번까지
+           다시 여쭙습니다. 세 번 모두 답이 없으면 위급으로 보고 보호자에게 알립니다.
+     action: notify_caregiver
```

**여기가 핵심이다.** 청력 답변 하나가 **무응답의 의미를 바꾼다.** 잘 들으시는 분의 무응답은 위급 신호지만, 귀가 어두우신 분의 무응답은 아직 아무 뜻도 아니다. 프로필이 없으면 동행이는 이 둘을 구별할 방법이 없다.

**④ 복약 지침**

```diff
  instructions:
-   - text: 복약 시간 신호를 받으면 처방 정보를 바탕으로 먼저 다정하게 복용을 권합니다.
+   - text: 복약 시간 신호를 받으면 "혈압약 드실 시간이에요"라고 약 이름을 짚어
+           다정하게 권합니다. (저녁 8시, 1정)
    - text: 카메라로 복용이 확인되면 "잘하셨어요!"처럼 따뜻하게 칭찬합니다.
    - text: 아직 복용하지 않았다면 다그치지 않고 부드럽게 다시 권유합니다.
+   - text: 드셨는지 기억하지 못하실 수 있습니다. 다그치거나 되묻지 말고,
+           기록을 확인해 "아까 여덟 시에 드셨어요"라고 대신 알려드립니다.
```

**⑤ 알림 내용** — `notify_caregiver` 호출 시 전달 문구에 프로필 맥락이 실린다.

> 기존: "낙상이 감지되었습니다. 응답이 없습니다."
> 이후: "낙상이 감지되었습니다. 세 번 여쭀지만 답이 없으십니다. **평소 청력이 좋지 않으셔서 못 들으신 것일 수도 있습니다.**"

보호자가 **119를 부를지 직접 전화해볼지** 판단하는 데 이 한 줄이 결정적이다.

#### 슬롯별 착지점 요약

| 슬롯 | 착지 |
|---|---|
| `hearing` | ① 말 속도 · ③ **무응답 판정 기준** · ⑤ 알림 맥락 |
| `mobility` | ① 움직임 권유 신중 · ② `min_confidence` 하향 + 보행기 단서 |
| `risk_places` | ② `detect_prompt` 보강 |
| `memory` | ① 반복 질문 대응 · ③ 복약 기록 확인 지침 |
| `talkativeness` | ① 대화 개시 빈도 |
| `help_attitude` | ① 말투 ("제가 해드릴게요" → "이렇게 하시면 됩니다") |
| `medications` | ③ 약 이름·시간이 지침에 박힘 · 시나리오 활성화 |
| `interests`·`family` | ① 대화 소재 |

**모든 슬롯이 최소 한 곳에 착지한다.** 착지점 없는 질문은 묻지 않는다 — §3.1에서 시력·지병을 인터뷰에 넣지 않고 프로필 페이지의 선택 항목으로만 둔 이유다.

---

## 7. 검토와 배포 — 이미 있는 것을 쓴다

```
인터뷰 결과
   │
   ├─ 프로필 사실  ─────────→  profiles 테이블  →  즉시(다음 세션부터) 반영
   │
   └─ 설정 제안   ─────────→  save_draft()  →  배포 화면  →  publish()
                                                   │
                                          diff()가 이미 변경 내역을 보여줌
```

`admin_store.diff()`가 `{text, tier}`를 돌려주고 tier가 `instant`/`next`로 나뉘어 있으므로, 온보딩 마지막 화면은 **새 화면이 아니라 기존 배포 화면에 문구만 얹은 것**이면 된다.

> **이렇게 준비했어요**
> 어머님 정보를 바탕으로 아래를 준비했습니다. 확인하고 시작해 주세요.
>
> · 낙상 감지 — 켜짐, 민감도 높임(거동이 불편하셔서)
> · 낙상 감지 — 지침 1개 추가: "세 번 물어도 답이 없으면 보호자에게 알린다"
> · 복약 확인 — 켜짐, 혈압약 저녁 8시
>
> [시작하기]   [고칠게요]

---

## 8. 지속적 개인화 — 온보딩은 시작일 뿐

`summarizer.py`가 이미 세션마다 **"요청했는데 이뤄지지 않은 것"** 을 뽑고 있다. 이게 온보딩보다 정확한 개인화 재료다 — 자녀의 기억이 아니라 실제로 일어난 일이니까.

```
세션 종료 → summarize() → unfulfilled[]  ─┐
                          notable[]       ├→ 누적 → 패턴 감지 → 제안
                          mood            ─┘
```

제안 예시:

- `unfulfilled`에 "물 좀 갖다줘"가 3회 이상 → **"물 챙기기 알림을 추가할까요?"**
- `notable`에 "무릎 아프다고 하심"이 반복 → **"프로필에 무릎 통증을 추가할까요?"**
- `mood`가 2주 연속 "가라앉음" → **"보호자에게 알릴까요?"**

이건 온보딩 이후 단계라 §11의 마지막 순서에 둔다. 다만 **데이터는 이미 쌓이고 있다** — 지금 시작해도 과거 데이터가 있다.

---

## 9. 화면

### 9.1 온보딩 (첫 로그인 시 1회, 언제든 중단 가능)

| # | 화면 | 내용 |
|---|---|---|
| 1 | 환영 | "어머님/아버님에 대해 3분만 알려주세요. 언제든 나중에 채우셔도 됩니다." + [건너뛰기] |
| 2 | 기본 | 성함 / 출생년도 / 호칭(칩 선택) / 혼자 지내시나요 |
| 3 | 카드 | 무엇을 지켜볼까요 — 카탈로그 카드 다중선택. 낙상·복약 기본 체크 |
| 4 | 인터뷰 | 채팅 UI. 동행이 말풍선 + 자녀 입력. [이만 마칠게요] 항상 노출 |
| 5 | 사진 | (복약 선택 시) 처방전·약봉투 촬영 또는 업로드. [건너뛰기] |
| 6 | 검토 | 배포 화면 재사용 + "이렇게 준비했어요" |

**모든 화면에 건너뛰기가 있다.** 원칙 3.

### 9.2 어르신 프로필 페이지 (상시)

사이드바 메뉴를 **어르신 / 시나리오 / 행동 / 알림 연락처 / 배포** 로 확장하고, 맨 위에 둔다. 여기서 언제든 프로필을 보고 고친다. 온보딩에서 건너뛴 항목은 "아직 안 알려주신 것" 으로 모아 보여주고, 채우면 어떤 게 좋아지는지 한 줄로 알려준다.

> **아직 안 알려주신 것**
> · 청력 — 알려주시면 어르신께 맞는 속도로 말씀드릴 수 있어요
> · 좋아하시는 것 — 대화가 끊길 때 꺼낼 이야깃거리가 됩니다

기존 화면들과 톤을 맞춘다 (26px 루트, `Field`/`Button`/`inputCls` 재사용).

---

## 10. 서버 API

| 메서드 | 경로 | 하는 일 |
|---|---|---|
| GET | `/api/admin/profile` | 프로필 조회 |
| PUT | `/api/admin/profile` | 프로필 저장 (즉시 반영, 이벤트 로그) |
| GET | `/api/admin/catalog` | 시나리오 카탈로그 (카드 화면용) |
| POST | `/api/admin/onboarding/chat` | `{messages[]}` → `{reply, done}` — 인터뷰 한 턴 |
| POST | `/api/admin/onboarding/extract` | `{messages[]}` → `{profile_patch, suggestions[]}` |
| POST | `/api/admin/onboarding/prescription` | 이미지 → `{medications[]}` |
| POST | `/api/admin/onboarding/apply` | 제안 확정 → 프로필 저장 + `save_draft()` |

### 인터뷰 구현 — 묻는 모델과 뽑는 모델을 나눈다

한 모델에게 "대화하면서 JSON도 채워라"고 하면 둘 다 나빠진다. 나눈다.

```
POST /chat     → 대화만. 다음 질문 하나를 생성. 필수 항목이 다 나오면 done=true
POST /extract  → 대화 전체를 한 번에 읽고 JSON 추출 (summarizer.py와 동일한 패턴)
```

`/extract`는 `summarizer.py`를 거의 그대로 쓴다 — 클라이언트, 모델(`meta-llama/llama-4-scout`, groq 라우팅), `_parse_json()`, 실패 시 조용히 포기하는 처리까지 동일. **프롬프트만 다르다.**

인터뷰 종료 조건: 필수 항목(청력·거동·기억·성향) 확보 / 8턴 초과 / 자녀가 중단. **셋 중 먼저 오는 것.**

---

## 11. 구현 순서

각 단계는 **독립적으로 동작하고 그 자체로 가치가 있다.** 중간에 멈춰도 망가지지 않는다.

| # | 단계 | 산출물 | 왜 이 순서인가 |
|---|---|---|---|
| 1 | **프로필 스키마 + 저장 + 페르소나 주입** | `profiles` 테이블, `build_profile_block()`, 프로필 페이지(폼) | 인터뷰 없이 폼만으로도 **대화가 즉시 달라진다.** 위험 0. 나머지 전부가 이 위에 얹힌다 |
| 2 | **온보딩 1·2단계 (기본 + 카드)** | 온보딩 라우트, 카탈로그 | 첫 로그인에 빈 화면을 보여주지 않게 된다 |
| 3 | **규칙 기반 조정 (레벨 2)** | `agent/personalize.py`, draft 반영 | LLM 없이 프로필이 설정을 바꾸기 시작 |
| 4 | **대화형 인터뷰 (레벨 3)** | `/chat`, `/extract`, 채팅 UI | 요약기 재활용이라 구현이 짧다. **시연에서 가장 그림이 되는 부분** |
| 5 | **처방전 사진** | `/prescription`, VLM 프롬프트 | 있으면 좋지만 없어도 손으로 입력 가능 |
| 6 | **요약 → 프로필 갱신 제안** | 누적 분석, 제안 카드 | 데이터가 쌓여야 의미가 있다 |

**1번만 해도 제품이 달라진다.** 거기서 시작한다.

---

## 12. 하지 않을 것

| | 이유 |
|---|---|
| **자녀가 페르소나 원문을 편집** | 안전 원칙("위급하면 알린다", "복용량 조절 안 한다")까지 지울 수 있다. 열더라도 호칭·말투 정도만 옵션으로 |
| **LLM이 시나리오를 자유 생성** | `detect_prompt`는 오탐/미탐을 좌우해서 튜닝이 필요하다. 카탈로그에서 고르게 한다 (§6) |
| **어르신 본인에게 인터뷰** | 첫 대화부터 심문이 된다. 정보는 자녀에게서 받고, 어르신께는 대화 중에 자연스럽게 알아간다 |
| **프로필을 다 채워야 시작 가능** | 원칙 3. 빈 프로필이어도 지금과 똑같이 동작해야 한다 |

---

## 13. 개인정보

프로필에는 **건강 정보(지병, 복용 약, 인지 상태)** 가 들어간다 — 민감정보다.

- 저장은 서버 로컬 SQLite에만. 외부로 나가지 않는다 (대화 요약과 동일한 취급)
- 처방전 이미지는 **추출 후 즉시 폐기**하고 텍스트만 남긴다
- 프로필 페이지에 **전체 삭제** 버튼을 둔다
- 인터뷰 대화 원문은 추출이 끝나면 버린다 (요약기와 같은 방침)

---

## 14. 손대는 파일

| 파일 | 변경 |
|---|---|
| `drone-agent-app/admin_store.py` | `profiles` 테이블, `get_profile()`, `save_profile()` |
| `drone-agent-app/agent/persona.py` | `build_profile_block()`, `build_unified_persona(..., profile=None)` |
| `drone-agent-app/agent/personalize.py` | **신규** — 레벨 2 규칙표, 프로필 → 설정 변환 |
| `drone-agent-app/agent/interviewer.py` | **신규** — 인터뷰 대화 + 추출 (`summarizer.py` 패턴) |
| `drone-agent-app/agent/catalog.py` | **신규** — 시나리오 카탈로그 (튜닝된 detect_prompt 포함) |
| `drone-agent-app/main.py` | 연결 시 `get_profile()` 호출, 온보딩 API 라우트 |
| `drone-agent-app/memory.py` | 프로필 테이블에서 읽도록 변경, 복약 로그만 자체 보관 |
| `admin-console/frontend/src/pages/Onboarding*.jsx` | **신규** — 6화면 |
| `admin-console/frontend/src/pages/Profile.jsx` | **신규** — 상시 프로필 페이지 |
| `admin-console/frontend/src/components/Layout.jsx` | 사이드바에 "어르신" 추가 |

---

## 15. 남은 큰 문제 — 콘솔이 아직 목 데이터를 쓴다

`admin-console/frontend/src/store.jsx`는 **localStorage 목 스토어**다. 콘솔에서 배포해도 에이전트에 닿지 않는다. `/api/admin` 엔드포인트는 서버에 있지만 프런트가 안 쓰고 있다.

**이 기획을 구현하기 전에 이걸 먼저 연결해야 한다.** 안 그러면 온보딩으로 만든 프로필이 브라우저 localStorage에만 남고 동행이는 영영 못 본다.

순서상 §11의 1번보다 앞이다.

---

## 16. 참고한 표준 평가 도구

노인 돌봄 현장에는 검증된 설문 도구가 이미 많다. **문항을 그대로 쓰지는 않지만(§16.3), 설계 원칙은 빌려올 게 많다.**

### 16.1 도구 목록

| 도구 | 규모 | 누가 답하나 | 우리에게 쓸모 |
|---|---|---|---|
| **AD8** (Washington Univ.) | 8문항 예/아니오 | **보호자(정보제공자)** | 우리와 상황이 정확히 같다. 문항 형식의 원본 |
| **KDSQ-C** (한국형 치매선별질문지) | 15문항 | 본인 또는 보호자 | 국민건강보험 일반검진에 포함. 기억력·기타 인지·복잡한 일상작업 |
| **장기요양인정조사표** (건보공단) | 12영역 90항목 (점수 산정은 5영역 52항목) | 훈련받은 조사원 방문 | **영역 구분과 항목명의 표준.** 특히 "행동변화" 영역 |
| **Katz ADL / Lawton IADL** | 6 / 8문항 | 보호자 | 기능 저하를 두 층으로 나누는 방식 |
| **Morse Fall Scale** | 6항목 | 의료진 | 낙상 위험 항목 구성 |
| **SGDS-K** (한국형 노인우울척도 단축형) | 15문항 예/아니오 | 본인 | 정서 영역, 이분형 응답 |
| **K-FRAIL** | 5문항 | 본인/보호자 | 짧은 도구의 표본 |

### 16.2 얻은 인사이트와 반영

**① 절대 수준이 아니라 "변화"를 묻는다 (AD8)**
AD8의 8문항이 전부 *"Yes, A change"* — 예전과 비교한 변화를 묻고, 기간 제한도 없다. 절단점은 2점.
→ **반영:** `memory` 문항을 "예전에 비해 …" 형태로 수정(§3.1).

**② ADL과 IADL은 다른 층이다 (장기요양인정조사표 · Lawton)**
표준은 기능을 둘로 나눈다. **기본(ADL)** — 옷 입기, 세수, 목욕, 식사, 화장실 사용, 일어나 앉기. **수단적(IADL)** — 전화, 장보기, 요리, 교통수단, 금전 관리, **약 챙겨 먹기**.
**IADL이 먼저 무너진다.** 그리고 "약 챙겨 먹기"가 IADL 항목이라는 건, 우리 복약 시나리오의 존재 이유가 표준에 이미 있다는 뜻이다.
→ **반영:** 조건부 질문에 "약은 스스로 챙겨 드세요?" 추가(§3.1). 이 답이 복약 시나리오를 켤지 결정한다.

**③ "행동변화" 영역이 곧 시나리오 카탈로그다 (장기요양인정조사표)**
행동변화 영역 항목: 망상, 환각, 슬픈 상태·울기, **불규칙 수면·주야 혼돈**, **도움에 저항**, **서성거림·안절부절**, **밖으로 나가려 함**, 물건 망가뜨림, 의미 없는 반복행동, 돈·물건 감추기, 부적절한 옷 입기.
→ **반영:** §6의 시나리오 카탈로그를 이 목록에 맞춰 재정비한다. 내가 지어낸 "야간 배회"는 사실 표준 항목 세 개(불규칙 수면 + 서성거림 + 밖으로 나가려 함)의 조합이었다. **`help_attitude` 슬롯도 "도움에 저항"이라는 표준 항목에 근거가 있다.**

**④ 검증된 도구는 짧다**
AD8 8문항, K-FRAIL 5문항, GDS 단축형 15문항. 90항목짜리 장기요양인정조사표는 **훈련받은 조사원이 방문해서** 작성하는 것이다.
→ **반영:** 우리는 웹에서 자녀 혼자 한다. AD8 급(10문항 이하)이 상한이다. **필수 6 + 조건부 2 + 마무리 2를 넘기지 않는다.**

**⑤ 응답은 이분형 또는 3~4지선다**
AD8도 SGDS-K도 예/아니오다. 노인 대상 도구가 리커트 5점을 피하는 이유는 응답 부담이다.
→ **반영:** 기존 선택지 3~4개 구조 유지. **5점 척도로 늘리지 않는다.**

**⑥ 낙상력은 낙상 평가의 첫 항목이다 (Morse Fall Scale)**
Morse Fall Scale의 1번 항목이 낙상 과거력(History of falling)이다. 우리 질문에 **없었다.**
→ **반영:** `fall_history` 를 필수 슬롯으로 추가(§3.1). 있으면 낙상 시나리오를 강제 활성화하고 `min_confidence`를 더 낮춘다.

**⑦ 다약제 복용(polypharmacy)은 그 자체로 위험 인자**
복용 약이 5종 이상이면 낙상·부작용 위험이 올라간다. 약 **종류**보다 **개수**가 지표다.
→ **반영:** 별도 질문이 필요 없다. **처방전 사진에서 개수가 자동으로 세어진다**(§3 4단계). 5종 이상이면 낙상 위험 점수에 가산.

**⑧ 도구에는 절단점(cutoff)이 있다**
AD8은 2점 이상이면 인지 손상 의심. 개별 문항이 아니라 **합산 점수**로 판정한다.
→ **반영:** §6 레벨 2 규칙을 개별 조건 매칭이 아니라 **점수 합산**으로 바꾼다. 조건 하나하나가 설정을 건드리면 서로 충돌하고 최종 상태를 예측하기 어렵다.

```
낙상 위험 점수 = 낙상력(2) + 거동 불편(2) + 보행기·휠체어(1)
               + 다약제 5종↑(1) + 혼자 거주(1) + 인지 저하(1)

0~1점 → min_confidence 0.7 (기본)
2~3점 → 0.65
4점↑  → 0.6  +  detect_prompt에 위험 장소 강조 추가
```

**⑨ 빠진 영역 — 정서와 사회적 고립**
포괄적 노인평가(CGA)는 신체·인지·정서·사회·영양·약물·기능을 함께 본다. 우리 프로필에는 **정서와 사회**가 `mood_recent` 한 칸뿐이다. **외로움 완화가 이 제품의 존재 이유인데 측정 항목이 없다.**
→ **반영 보류.** 마무리 자유 질문("제가 꼭 알아야 할 게 더 있을까요?")과 대화 요약의 `mood` 누적(§8)으로 우선 대체하고, 필요하면 나중에 SGDS-K 스타일 1~2문항을 추가한다. 온보딩을 늘리는 것보다 §8의 누적 관찰이 정확하다.

### 16.3 경계 — 우리는 선별검사를 하지 않는다

AD8과 KDSQ-C는 **치매 선별 도구**다. 문항을 그대로 가져다 점수를 매기면 우리 제품이 *"치매 의심"* 판정을 내리는 셈이 되고, 이건 두 가지와 충돌한다.

1. `PERSONA_BASE`의 안전 원칙 — *"진단하거나 약을 바꾸라고 하지 않습니다"*
2. 검증된 도구는 **검증된 조건(대면, 훈련된 조사자, 정해진 문항 순서)** 에서만 성능이 보장된다. 웹 폼에 옮겨 적은 순간 그 근거가 사라진다.

**그래서 이렇게 한다.**

- 문항 **구조와 설계 원칙**만 빌린다 (변화형 질문, 이분형 응답, 짧은 분량)
- 원 도구의 문항을 그대로 옮기지 않고, 도구 이름도 화면에 쓰지 않는다
- 점수(§16.2 ⑧)는 **내부 설정 조정용**이며 **보호자에게 보여주지 않는다**. "낙상 위험 4점"이 아니라 *"거동이 불편하셔서 낙상 감지를 더 민감하게 맞췄어요"* 로 표시한다
- 어떤 화면에서도 진단명·의심 소견을 말하지 않는다

정식 평가가 필요해 보이면 **판정하지 말고 안내만 한다** — *"가까운 치매안심센터에서 무료로 검사받으실 수 있어요."*

**확인이 필요한 것:** Morse Fall Scale 항목 구성, K-FRAIL·SGDS-K 문항 수, 다약제 기준(5종)은 통용되는 값으로 적었으나 원문 대조는 하지 않았다. 설계 근거로만 쓰고 외부 인용 시에는 확인이 필요하다.
